# Kairos — pip install -r requirements.txt
#
# This file mirrors pyproject.toml [project.dependencies] for environments
# that need a plain requirements file (e.g. Docker, CI).
#
# Preferred install method: pip install -e '.[train,crypto]'
# (gives you CLI entry points + optional extras in one go)

# ---- core ----
numpy
pandas>=2.0
pyarrow>=14
torch>=2.0
einops>=0.8
tqdm>=4.65
safetensors>=0.4
huggingface_hub>=0.24
akshare>=1.14
matplotlib>=3.7

# ---- optional: train (pip install -e '.[train]') ----
# scipy>=1.10
# comet-ml>=3.39

# ---- optional: crypto (pip install -e '.[crypto]') ----
# ccxt>=4.3
# python-dotenv>=1.0

# ---- optional: serve (pip install -e '.[serve]') ----
# fastapi>=0.110
# uvicorn[standard]>=0.29
# pydantic>=2.5
